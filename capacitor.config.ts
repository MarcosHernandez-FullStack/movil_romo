import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.softbrilliance.frontendng20',
  appName: 'FrontendNg20',
  webDir: 'dist/frontend-ng20/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;